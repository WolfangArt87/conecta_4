# 🔵 Conecta 4 en C++ (Modo Consola)

Este es un proyecto clásico del juego Conecta 4, implementado en C++ para jugar directamente en la terminal o consola. Está diseñado para dos jugadores humanos (X vs. O) que se turnan para soltar sus fichas en un tablero de 6 filas por 7 columnas.

## 🚀 Compilación y Ejecución

El programa está escrito en C++ estándar (C++11 o posterior).

### En Visual Studio (Recomendado)

Simplemente abre el archivo `.sln` con Visual Studio 2022 (o una versión compatible) y presiona **F5** (o el botón "Iniciar") para compilar y ejecutar el proyecto.

### Con g++ / clang (Terminal)

Si prefieres compilarlo manualmente desde una terminal, puedes usar:

```bash
# Asumiendo que el archivo se llama conecta4.cpp o main.cpp
g++ -o conecta4 tu_archivo.cpp -std=c++11

# Para ejecutar
./conecta4
```
